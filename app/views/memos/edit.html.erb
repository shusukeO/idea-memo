<h1>Editing Memo</h1>

<%= render 'form', memo: @memo %>

<div>
    メモ
</div>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Content</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @memos.each do |memo| %>
      <tr>
        <td><%= memo.user.email %></td>
        <td><%= memo.content %></td>
        
      </tr>
    <% end %>
  </tbody>
</table>

<%# コメントアウト %>
<% if false %>
<%= link_to 'Show', @memo %> |
<%= link_to 'Back', memos_path %>
<% end %>

<%= link_to 'Back', memos_path %>
